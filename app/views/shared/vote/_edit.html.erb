<%= form_for(parents_vote(parent), url: vote_path(parents_vote(parent)), action: "update") do |f| %>
  <%= f.hidden_field :value, value: 1 %>
  <%= f.button type: 'submit', class: "my-button clickable", id: "up", disabled: parents_vote(parent)&.up_vote? do %>
    <%= tag.i class: "fa fa-chevron-circle-up" %>
  <% end %>
<% end %>
<%= form_for(parents_vote(parent), url: vote_path(parents_vote(parent)), action: "update") do |f| %>
  <%= f.hidden_field :value, value: -1 %>
  <%= f.button type: 'submit', class: "my-button clickable", id: "down", disabled: parents_vote(parent)&.down_vote? do %>
    <%= tag.i class: "fa fa-chevron-circle-down" %>
  <% end %>
<% end %>
