<%= form_for(vote, url: vote_path(vote), action: "update") do |f| %>
  <%= f.button type: 'submit', class: "my-button clickable", id: vote_type do %>
    <%= tag.i class: "fa fa-chevron-circle-#{vote_type}" %>
  <% end %>
<% end %>
