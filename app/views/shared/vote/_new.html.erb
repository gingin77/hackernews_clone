<%= button_to(votes_path, params: { vote: { voteable_type: parent.class.to_s, voteable_id: parent.id, value: 1 }}) do %>
  <%= tag.i class: "fa fa-chevron-circle-up" %>
<% end %>


<%#= button_to(:submit, { :action => :create }, params: @event_registration.as_json(root: true, include: :participants)) %>
