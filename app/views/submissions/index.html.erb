<% posts.map do |s| %>
  <br />
  <% if s.text.blank? %>
    <%= link_to(s.title, s.url) %>
  <% else %>
    <%= s.limit_text %>
  <% end %>
  <br />
  <p>by <%= link_to(s.user.name, profile_path(s.user_id)) %> | posted <%= link_to(time_ago_in_words(s.created_at), submission_path(s.id)) %>  ago
  </p>
<% end %>
