<% require "benchmark" %>
  <% n = 50000 %>
  <%= Benchmark.bm do |x| %>
    <% x.report do %>
      <%= render partial: "shared/vote/new",
        locals: {
                  parent: parent,
                  vote: parent.users_vote(current_user),
                  one: 1,
                  b_vote_type: "up"
                } %>

      <%#= render partial: "shared/vote/edit",
        locals: {
                  parent: parent,
                  vote: parent.users_vote(current_user),
                  one: -1,
                  b_vote_type: "down"
                } %>
    <% end %>
<% end %>
